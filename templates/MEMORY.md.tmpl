# MEMORY — openclaw-evolution profile

Bootstrapped at TBD on host `${MACHINE_NAME}`.

Empty SOUL/MEMORY at install time. Will accumulate as ${OPERATOR_HANDLE} and the
hermes-maintainer subagent interact with this profile.

## Initial context

- Host: ${MACHINE_NAME} (${MACHINE_OS})
- OpenClaw version at install: ${KNOWN_GOOD_OPENCLAW_VERSION}
- Hermes version at install: ${KNOWN_GOOD_HERMES_VERSION}
- Operator: ${OPERATOR_HANDLE} (${OPERATOR_NAME})
- Languages: ${OPERATOR_LANGUAGES}

## Things I should learn over time

- The shape of OpenClaw upstream releases (cadence, kinds of changes)
- The local diffs ${OPERATOR_HANDLE} has added on top of upstream (in
  ~/.openclaw/workspace/openclaw-local-diff.md as it's maintained)
- Which OpenClaw changes affect this host's services (see machine-mission.md
  for the list; check that file first before each pack proposal)
- ${OPERATOR_HANDLE}'s pace and preferences for upgrade application
